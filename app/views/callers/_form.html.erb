<%= form_with(model: caller, local: true) do |form| %>
  <% if caller.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(caller.errors.count, "error") %> prohibited this caller from being saved:</h2>

      <ul>
      <% caller.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name, id: :caller_name %>
  </div>

  <div class="field">
    <%= form.label :original_title %>
    <%= form.text_area :original_title, id: :caller_original_title %>
  </div>

  <div class="field">
    <%= form.label :notes %>
    <%= form.text_area :notes, id: :caller_notes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
